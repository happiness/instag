services:
  instag.importer:
    class: Drupal\instag\InstagramImporter
    arguments: [ '@config.factory', '@logger.channel.instag', '@file.repository', '@file_system', '@http_client' ]
  logger.channel.instag:
    parent: logger.channel_base
    arguments: [ 'instag' ]
